#include <stdio.h>
/**
 * main - Entry point
 * @argc: argument count
 * @argv: argument array
 * Return: always Success
 */
int main(int argc, char *argv[])
{
	if (argc >= 1)
	printf("%s\n", argv[0]);
	return (1);
}
